# Base URL for heavy media (hero videos, product PNGs) served from Cloudflare
# R2 instead of being bundled in the repo/public folder. See public/hero and
# public/lights for the paths these get appended to (e.g. /hero/01-intro.mp4).
#
# Local dev default (Zp.akyriakouu account, "solis-assets" bucket, r2.dev dev URL):
NEXT_PUBLIC_ASSET_BASE_URL=https://pub-471c128943474ff0a49ebadc867b4477.r2.dev
